<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bowensun.film.repository.UserInfoDao">

    <resultMap id="BaseResultMap" type="com.bowensun.film.domain.UserInfo">
        <id column="uid" property="uid"/>
        <result column="username" property="username"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="state" property="state"/>
    </resultMap>

    <sql id="Base_Column_List">
      uid,username,`name`,`password`,`salt`,`state`
    </sql>

    <!--新增用户-->
    <insert id="userAdd">
        insert into
          user_info(`name`, password, salt, state, username)
        value ('普通用户', #{user.password}, '123', 1, #{user.username})
    </insert>

    <!--查询列表-->
    <select id="selectByUserName" resultMap="BaseResultMap">
        select
          <include refid="Base_Column_List"/>
        from
          `user_info`
        where
          userName = #{userName}
    </select>

    <select id="selectUserInfoList" resultMap="BaseResultMap">
        select
          <include refid="Base_Column_List"/>
        from
          `user_info`
    </select>
</mapper>